{
  "name": "vilify",
  "version": "0.6.34",
  "description": "Vim-style focus mode for websites",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.20.0",
    "jsdom": "^28.0.0",
    "typescript": "^5.9.3",
    "vitest": "^2.0.0"
  }
}

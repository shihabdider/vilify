{
  "name": "vilify",
  "version": "0.5.95",
  "description": "Vim-style focus mode for websites",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "jsdom": "^28.0.0",
    "vitest": "^2.0.0"
  }
}
